apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: shipment-manager
  namespace : streams
  labels: 
      keel.sh/policy: force
      keel.sh/approvals: "1"
spec:
  replicas: 1 # tells deployment to run 2 pods matching the template
  template: # create pods using pod definition in this template
    metadata:
      labels:
        app: shipment-manager
    spec:
      containers:
      - name: shipment-manager
        image: gcr.io/fretron-server/shipment-manager:latest
        ports:
        - containerPort: 8089