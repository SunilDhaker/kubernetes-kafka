kind: Service
apiVersion: v1
metadata:
  name: registry-manager
  namespace : streams
spec:
  type: NodePort
  selector:
    app: registry-manager
  ports:
  - protocol: TCP
    name : proto1
    port: 8087